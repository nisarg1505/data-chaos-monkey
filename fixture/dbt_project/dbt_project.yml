name: 'chaos_fixture'
version: '1.0.0'
profile: 'chaos_fixture'
model-paths: ["models"]
seed-paths: ["seeds"]
target-path: "target"
models:
  chaos_fixture:
    staging:
      +materialized: view
    marts:
      +materialized: table